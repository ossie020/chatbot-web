export const API_BASE = import.meta.env.VITE_API_BASE_URL
export const KEYS = {
  COOKIE: 'gensoul-cookie',
  DARK: 'dark',
  NSFW: 'nsfw',
}

export const REGS = {
  EMAIL: /^[A-Za-z0-9+_.-]+@(.+)$/,
}
